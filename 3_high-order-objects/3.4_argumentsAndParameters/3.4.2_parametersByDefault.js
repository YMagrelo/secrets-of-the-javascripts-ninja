function performAction(ninja, action = 'skulking') {
  return console.log(ninja + ' ' + action);
}

performAction('Fuma');
performAction('Yoshi');
performAction('Hattori');
performAction('Yagyu', 'sneaking');
